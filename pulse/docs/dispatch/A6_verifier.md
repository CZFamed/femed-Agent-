# 派工单 · A6 Verifier（独立验证域）

> 任务名：`a6_verifier`　｜　波次：**W2**　｜　可写目录：`pulse/tests/`（唯一所有者）
> 开工前必读：`AGENTS.md` → 本派工单 → `pulse/contracts/INTERFACES.md` 全文

## 1. 边界

| | 内容 |
| --- | --- |
| 可写 | `pulse/tests/` |
| 只读 | 其余全部（`pulse/services/`、`pulse/shared/`、`pulse/contracts/`） |
| 禁止 | **修改被测代码**（发现 bug 只报告，不代改）；碰 git；写各域的 `services/*/tests/` |

## 2. 交付目标（一句话）

对全仓给出**独立、可复现**的验证结论；你说是"过"才算过。

## 3. 任务包

| ID | 任务 | 完成定义（DoD） | 证据 |
| --- | --- | --- | --- |
| W2-A6-1 | **契约一致性校验** | 断言代码中的字段名/枚举与 `INTERFACES.md` 零偏差（平台枚举、状态机、`ErrorClass`、options 必填项、ID 前缀） | 测试命令 + 通过数 |
| W2-A6-2 | 集成测试 | 假 Adapter 端到端：`brief → variant → 合规 → 审批 → 排期 → 发布 → 回执` | 通过的端到端用例清单 |
| W2-A6-3 | 风控演练 | 四类：限流 / 认证失效 / 政策拒绝 / 异步超时。各自断言**降级行为**而非仅"没崩" | 每类一条用例 + 预期状态 |
| W2-A6-4 | 幂等演练 | 重复投递不产生重复发布（`publish_jobs` 唯一索引 + `find_existing()` 双层） | 重复投递用例 |
| W2-A6-5 | 生成质量回归框架 | 固定 brief 集打分；框架可跑，**不追求真实生成质量** | 框架 + 一轮基线输出 |

## 4. 你补的是一个已存在的缺口

> **[2026-09-11 更新｜root]** 本节前提已变：root 已在 `pulse/shared/tests/test_contract_baseline.py`
> 交付 **21 项**基线校验（全绿；契约 v1.1 起为 21 项）。因此 W2-A6-1 **不再是"从零建立基线"**，而是**独立复核 + 补盲区**：
> ① 不得照抄那份断言，必须独立编写（否则失去独立性）；② 要覆盖基线未覆盖的部分——状态机迁移表、
> `media_processing` 轮询路径、`SemiAutoBundle` 四字段、`to_dict()` 往返、跨域字段一致性；
> ③ 若发现基线断言本身写错 → 消息 root 并附契约条款编号，**不要改** `pulse/shared/tests/`（root 所有）。

早先 `AGENTS.md` 声称"`pulse/shared/` 契约层已通过 16 项校验"，但仓库中当时并不存在测试文件，
`pytest` 收集到 0 条用例；该表述随后又被改成"当前没有任何自动化测试"。**两种说法现在都已作废**——
以本节开头 root 的更新与 `AGENTS.md` §5.4 为准：基线是 21 项，已交付。

**因此 W2-A6-1 是补齐这项承诺的正式交付物**：契约层要有真实、可复现的校验用例，覆盖面至少包含
`Platform` 白名单、`LicenseStatus`（禁止 `pending` 发布）、`scheduled_at` 时区偏移、平台必填 options、
`compliance.blocked` 硬拦截、hashtag 规范、ID 前缀、`PublishResult` 的"受理 ≠ 发布"断言。

## 5. 关键约束

1. **独立性是你的全部价值。** 不得为了让测试变绿而放宽断言，也不得修改被测代码。发现问题 → 消息对应域 agent + 抄送 root，附最小复现。
2. **并发跑测试加 `-p no:cacheprovider`**（多个 agent 共享一个 `.pytest_cache`）。
3. 测试必须**可离线运行**：不发真实网络请求、不依赖真实 Redis、不依赖真实模型。
4. 覆盖率目标：关键函数 ≥ 80%。覆盖率不是目的，**但"关键函数未覆盖"要能被你说出来是哪些**。

## 6. 交付

按 `README.md` §4 模板回报 root，另附**验证报告**：通过的用例清单、失败的用例及归属域、每条失败的最小复现步骤。
