# 变更记录

本文件记录 Pulse 的**对外可见版本**变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.16.0] — 2026-09-20

**修复"库里一堆图却一张也召回不来"：位次召回改为逐级放宽、保证数量**（媒体库契约 v1.7.2 → v1.7.3）。

### 问题（实测数据）

召回要连过四道闸：品类 → 关键词 → **同档 95%** → 冷却。叠加之后，真实库（490 条素材）的实际产出：

| 平台 | 修复前 | 修复后 |
| --- | --- | --- |
| LinkedIn | **1 / 4** | 4 / 4 |
| Facebook | 3 / 4 | 4 / 4 |
| TikTok | 4 / 6 | 6 / 6 |
| VK | 4 / 4 | 4 / 4 |

最刺眼的是 LinkedIn 位次 4：**157 张候选被同档阈值砍到 4 张**，再被冷却清掉 14 张里的 4 张 → 一张不剩。

### 修复

- **逐级放宽补足数量**：`strict`（品类+关键词+同档）→ `same_category`（同品类）→
  `category_any_keyword`（同品类不限关键词）→ `whole_library`（全库）。
  够数就停，绝不留空。
- **按档位分批、各位次公平取用**：每轮所有位次都先在当前档跑一遍，
  避免靠前的位次用放宽档把靠后位次唯一匹配的那张图抢走；同一条素材不在同一帖里占两个位次。
- **放宽必须可见**：每个位次回传 `level` / `level_label` / `relaxed`，
  页面写明"这一格没有精准素材，这张是放宽补位，建议补拍"——
  不允许把补位图当精准匹配展示。
- **数量对不上如实报**：新增 `requested` / `shortfall`，页面显示"只召回到 X / Y 张"。
- **冷却不让路**：进冷却就是 15 天（B1/B12 不变），不因为凑数破例。

### 顺带修掉一个环境级缺陷

`ffmpeg_executable()` 原先只依赖 `imageio_ffmpeg.get_ffmpeg_exe()`，而它内部靠
"试跑一次 `ffmpeg -version`"判定二进制可用性，还带 `lru_cache`——
那次探测一旦被杀软/沙箱偶然拦住（Windows 上报 `WinError 5 拒绝访问`），
**结果会被记一整个进程**，于是整个控制台都以为"没有 ffmpeg"、视频再也入不了库。
现改为探测失败时**按路径**直接从 `imageio-ffmpeg/binaries/` 找二进制。

### 验证

- 真实库实测：四个平台全部足量（见上表），`shortfall` 全为 0。
- 全量测试 **360 项**全绿（新增 5 项：候选池阶梯、形态偏好、最宽松档、
  档位中文标注、窄位次靠全库档补足；另改写 1 项为"够料时必须填满 + 不够时如实报缺"两条）。

---

## [1.15.2] — 2026-09-15

**修复：双击启动器会留下两个控制台，旧进程继续吐旧页面**（上一版修复"看起来没生效"的真正原因）。

### 问题

1.15.1 修掉了「人员」入不了库的两道关卡，但用户重启后**页面还是旧的**。查下来：
机器上同时跑着**两个**控制台进程（一个是前一天 08:19 起的、一个是当天 13:14 起的），
两个都监听着 127.0.0.1:**8765**。

- 根因是 Windows 的 `SO_REUSEADDR` 语义：先启动的服务带 `SO_REUSEADDR`，
  后启动的进程**照样能 bind 成功**，于是两个进程同时"监听"同一端口，
  浏览器连上哪个全看运气。
- 启动器的端口探测恰恰是用 `bind` 试探的（`pick_port`），在 Windows 上被放行，
  所以它以为 8765 空闲、又起了一个。
- 于是"改了代码 → 用户重启 → 命中旧进程 → 还是旧页面"。页面 HTML 是进程启动时
  装载的，刷新浏览器没用。

### 修复

- `pick_port` 改成**先 connect 判活**：连得上就说明真有人占着，直接换端口。
- 新增 `probe_running_console` / `plan_start`：启动前先问端口上的 `/api/state`。
  - 同一版控制台已经在跑 → **只打开浏览器，不再起第二个进程**（重复双击变幂等）；
  - 跑着的是**旧版**（或老版本连 `version` 字段都没有）→ 在**新端口**启动最新版，
    并明确提示"请把旧的黑窗口关掉"。
- `/api/state` 新增 `version` 字段（启动器据此比对版本）。

### 验证

- 全量测试 **354 项**全绿（新增 7 项：SO_REUSEADDR 劫持下端口探测仍能识破、
  探针能读出同版本控制台、不会把别的程序错认成控制台、
  `plan_start` 的复用/换端口/未知版本三种决策、`/api/state` 暴露版本）。
- 现场复现与修后确认：杀掉两个旧实例 → 重启 → 运行中的控制台版本 `1.15.2`，
  页面里子类下拉**已无 `required`**，`/api/state` 的 `categories` 含「人员」；
  再对**正在运行**的控制台发一次页面同款请求（`process=人员` + 空子类）→
  识别 `vision`、品类 `人员`、子类为空、拿到入库凭据。

---

## [1.15.1] — 2026-09-15

**修复：品类选「人员」时无法入库**（没有子类的品类被两道关卡挡住）。

### 问题

在控制台里把品类选成「人员」（库里唯一没有子目录的品类）后，点「上传并入库」没有任何反应。

两道关卡各挡一次：

1. **前端**：子类下拉写死了 `required`，而「人员」只有一个 `value=""` 的「（无子类）」选项——
   浏览器把空值判成"未选择"，直接拦下整张表单（表现就是点了没反应）。
2. **后端**：`/api/describe` 把空的 `sub_process` 改写成"未分类"，于是
   `catalog.has("人员", "未分类")` 为假 → 用户手选的「人员」被丢掉 →
   页面提示"没能判断出品类"，人卡在这一步。

契约 §2.7 本来就写了"`sub_process` 可为空"，是实现没跟上。

### 修复

- 子类下拉**去掉 `required`**（品类本身仍必选）；空子类是合法值，不是"没填"。
- `/api/describe` **原样透传** `process` / `sub_process`，不再发明"未分类"；
  描述看不出品类时，正确回落到用户在页面上手选的品类（`category_source: manual`）。
- 入库链路（`/api/assets` → `MediaIngestor`）本来支持空子类，这次用 HTTP 层测试钉住。

### 验证

- 全量测试 **349 项**全绿（新增 2 项：子类下拉不得 required；HTTP 走
  `process=人员 & sub_process=` 时既要保住手选品类、又要落进 `人员/` 且不产生「未分类」）。
- 真实素材 + 真实视觉模型端到端：用库里的人员实拍 → 识别出 `人员`（`vision`）→
  拿凭据入库 → 描述落在 `RAG知识库/图片描述/人员/`、索引重建、**没有**多余的「未分类」目录。

---

## [1.15.0] — 2026-09-14

**支持 Photoshop 设计稿（`.psd` / `.psb`）：能入库、能识别、能预览、导出自动附 PNG**（媒体库契约 v1.7）。

### 问题

设计稿进不了素材库：`.psd` 不在入库白名单里，而且它**不是栅格格式**——
视觉接口只吃 PNG/JPEG，浏览器也不认识 Photoshop 文档，社媒更发不出去。
设计同事给的原稿因此只能在素材库外面流转。

### 修复

- **白名单**：`.psd` / `.psb` 按图片收，体积上限单独给 `MAX_PSD_BYTES`（200 MB，
  实拍图仍是 20 MB）——设计稿多层加无损通道，动辄上百 MB。
- **自己解 PSD，不装 Pillow**：新增 `pulse/services/media/psd.py`（纯标准库）。
  支持 PSD(1)/PSB(2)、位图/灰度/索引色/RGB/CMYK、8/16 位、原始与 RLE(PackBits) 压缩，
  有 alpha 通道时输出带透明 PNG。**不支持** 32 位浮点 / ZIP 压缩 / 多通道 / 双色调 / Lab，
  遇到即抛 `UnsupportedPsdError` 并给中文原因，不会悄悄给一张错图。
- **送模型 / 预览**：模型接口与浏览器都先拿到解出的合并图 PNG（最长边 1600 px 整数倍降采样），
  其余格式一律原样字节转发，零修饰口径不变。
- **导出**：原 `.psd` 与转换出的 `.png` **一起进包**，`说明.txt` 逐条注明
  "PSD 解出的合并图 → PNG，未做任何修饰"；解不出来时仍按字节复制原稿，只写"转换 PNG 失败"。
- 页面文案与上传框（`accept`）同步提示支持 PSD。

### 验证

- 全量测试 **347 项**全绿（新增 26 项：PSD 文件头/尺寸探测、原始与 RLE 解出的像素一致、
  RLE 重复段、灰度+alpha、CMYK→RGB、16 位取高字节、索引色调色板、降采样、PNG 结构校验、
  PSB 8 字节图层长度、真实长资源段跳过、不支持子集的中文报错、入库体积上限、导出附 PNG）。
- 端到端冒烟（真实调用视觉模型）：合成 PSD → 解出 64×48 合并图 → 转 PNG → 预览载荷
  `image/png` → 模型返回 `source=vision` 的描述，**4.6 秒**，证明链路可用。

---

## [1.14.0] — 2026-09-14

**素材库品类 / 子类调整同步：目录搬家不再打散控制台与平台位次**（媒体库契约不变，属数据与配置对齐）。

### 背景

业务侧重整了 `菲美得产品图片/` 的品类结构，RAG 描述库还停在旧目录上：

| 变化 | 内容 |
| --- | --- |
| 改名 | `铸件/阀体` → `铸件/壳体`（105 张）；`加工件/阀体` → `加工件/壳体`（30 张） |
| 挪窝 | `铸件/扫描` → `生产流程/扫描`（2 张）；`铸件/箱体_支座` 的浇注现场 → `生产流程/浇筑`（1 张） |
| 拆类 | `生产流程/涂装线` 拆出 `生产流程/黄模`（泡沫模样挂涂料 / 烘干，4 张） |
| 新建 | `生产流程` 下新增 7 个待补拍工序：下检 / 抛丸 / 清砂 / 熔炼 / 翻箱 / 造型 / 雕刻 |
| 素材进出 | 新增 `生产流程/发泡/2S4A0076.JPG`、`资质证书/` 5 张；删除 `铸件/管件/IMG_20220921_081133.jpg` |

### 修复

- **描述库跟着素材搬家**：142 份描述换目录 / 换文件名，216 份原地刷新头部
  （`source_file` / `source_path` / `process` / `sub_process`），共 153 份头部确实过期；
  重建 18 份 `00_汇总索引.md`，并给 7 个空工序补占位索引，控制台下拉框立刻跟上新品类。
- **孤儿描述隔离**：素材已不在库里的 2 份描述（`厂区_场景/厂房/20`、
  `铸件/管件/IMG_20220921_081133`）移出描述库，避免召回挑到"文件不存在的素材"。
- **asset_id 跟着迁移**：描述路径变了，素材 ID 就变，登记表（新图加权）与使用台账
  （15 天冷却）同步改写；已导出素材的冷却没有因为目录调整而丢。
- **位次口径跟上新品类**（`platforms.py`）：检测类位次把 `生产流程` 列入候选
  （扫描搬家后否则再也召回不到）；关键词补 `浇筑 / 黄模 / 涂料 / 浸涂 / 浆料`。

### 验证

- 全量测试 **321 项**全绿（新增 2 项：扫描搬去生产流程后检测位次仍能召回、
  浇筑与黄模两个新工序能被对应位次召回）。
- 真实素材库核对：358 份描述 ↔ 363 个媒体文件，**0 份描述找不到素材文件**；
  整库冷却中 12 张（含迁移 ID 后的两张）保持不变。

---

## [1.13.0] — 2026-09-13

**导出即进入冷却：点"导出这一帖的素材"后，包内素材开始计 15 天**（媒体库契约 v1.5 → v1.6）。

### 问题

导出**没有写台账**。冷却的唯一触发入口是每张素材卡片上的"标记已用于内容"按钮，
需要人工逐张去点。于是导出 → 召回 → 再导出，同一张图会被反复挑中，
页面"冷却中 N 张"永远是 0。

这与契约口径相反：契约 §2.1 写的是"素材**实际进入生成内容或发布**时调用
`RecallLedger.mark_used`"，而导出包就是原图 + 说明 + 文案，人拿着它去平台发——
**导出正是"进入发布"那一步**，本该触发冷却。

### 修复

- `/api/export` 成功后，把**真的复制进包**的素材写入台账；`content_id` = 导出目录名，
  同一次导出重复调用幂等，不会重复消耗冷却。
- 返回值新增 `used`（本次新计入冷却的素材 ID）与 `cooldown_days`；
  界面提示改为"已导出 N 个文件到：… ，M 张素材进入 15 天冷却"，并立即刷新冷却计数。
- **没复制成功的素材不记冷却**（文件缺失、复制失败），避免白扣 15 天。

### 验证

- 全量测试 **319 项**全绿（新增 3 项：导出后素材进入冷却、冷却期内不再被召回、
  没复制成功的素材不记冷却）。

---

## [1.12.0] — 2026-09-11

**先查重再识别：重复素材不再产生模型费用**（媒体库契约 v1.4 → v1.5）。

### 问题

原来的顺序是"**先识别、后查重**"：重复文件照样会被送去识别（图片约 2,000 token、
视频约 3,000 token），等识别费花完，才在入库那一步被拒。白花钱。

### 修复

- **把查重提到识别之前**。命中即直接返回"已在库中"，**完全不调用模型**。
- 查重范围不只是登记表：既有素材库（318 份描述、老图）**同样查**，按**内容哈希**判定，
  与文件名无关。
- **性能**：全库 1.34 GB / 336 个文件，不可能每次上传都重算哈希。
  实现为**先按字节数过滤**（`stat` 几百个文件仅数毫秒），只对尺寸相同的极少数文件真算哈希。

  实测（真实素材库）：

  | 场景 | 耗时 | 结果 |
  | --- | --- | --- |
  | 上传既有素材（2.16 MB 图） | **38 ms** | 命中，跳过识别 |
  | 上传既有视频（6.70 MB） | **41 ms** | 命中，跳过识别 |
  | 同一张图改 1 个字节 | 72 ms | 未命中，正常识别 |
  | 全新素材 | 65 ms | 未命中，正常识别 |

- **浏览器侧预查重**：页面用 `crypto.subtle` 先算 SHA-256 调 `/api/precheck`，
  命中则**连文件都不上传**——对 200 MB 的视频尤其省事。
  浏览器不支持时自动跳过；服务端在 `/api/describe` 里**再查一次**作为权威判定。

### 顺带修掉一个数据损失隐患

既能素材（老图）不在登记表里，所以光靠内容哈希查不出来。如果新上传一个**同名但内容不同**
的文件，原描述会被直接覆盖——**不可逆**。现在同名描述已存在时一律拒绝，
并明确写"拒绝覆盖"。

### 验证

- 全量测试 **316 项**全绿（新增 8 项：命中登记表/既有库都不调用模型、
  改一个字节不算重复、视频重复同样跳过、字节数过滤只哈希同尺寸文件、拒绝覆盖同名描述）。
- 真实素材库实测见上表。

---

## [1.11.0] — 2026-09-11

**实拍视频可被识别并入库**（媒体库契约 v1.3 → v1.4）。

### 先确认了一件事：接口不支持视频输入

实测三种送法都失败：

| 送法 | 结果 |
| --- | --- |
| `input_video` + `video_url` | `400 unknown variant input_video, expected one of input_text, o…` |
| `input_video` + `detail` | 同上 |
| 视频字节塞进 `input_image` | `400 unsupported image` |

所以视频必须**先抽静帧、再当多图**送模型。而本机原本没有任何 ffmpeg，
解 H.264 也不可能用纯 Python 做——新增依赖 **`imageio-ffmpeg`**（自带静态 ffmpeg 二进制）。

### token 实测（你关心的一点）

同一段实拍视频，不同抽帧配置：

| 抽帧配置 | 帧合计 | 输入 token |
| --- | --- | --- |
| 1 帧 @1280px | 132 KB | 791 |
| 4 帧 @1280px | 528 KB | 3,008 |
| **4 帧 @768px** | 261 KB | **1,184** |
| 6 帧 @768px | 393 KB | 1,750 |

服务端按像素量折算，所以**缩小尺寸比减少帧数更划算**。默认取 4 帧 @768px——
输入成本与一张原图（约 1,170）基本持平。

真实端到端一段 6.7MB / 2.2 秒视频：**输入 1,569 + 输出 1,481（含思考 1,190）= 3,050 token**，
耗时 12 秒。输出侧与图片同量级，**不需要额外调高描述上限**（4,000 够用，且已有截断自动放大）。

### 新增

- `pulse/services/media/video.py`：探测时长/分辨率、抽帧、错误分类。
  ffmpeg 来源优先级：`PULSE_FFMPEG` → `imageio-ffmpeg` → PATH。
- 描述器支持多帧输入（`describe_frames`）：系统提示里说明"这些是同一段视频的连续画面"，
  实测模型能正确写出"同一段约 2.2 秒视频的连续 4 帧，镜头自左向右平移"。
- 入库接受视频：描述文件按库里既有约定命名 **`<文件名带扩展>.md`**（如 `2.mp4.md`），
  头部 `content_type: "视频描述"`；视频不计入图片容量，单独统计。
- 控制台：上传接受 `image/*,video/*`；识别完成后显示**本次消耗的 token**
  （含思考 token），让成本可见。

### 配置

`PULSE_VIDEO_FRAMES`（默认 4）、`PULSE_VIDEO_FRAME_WIDTH`（默认 768）、`PULSE_FFMPEG`（可选）。

### 验证

- 真实视频端到端：抽帧 → 识别（`source=vision`）→ 入库为 `2.mp4.md`
  → 容量报告"视频 20 个、图片容量不变"。
- 仓库内新增 7KB 测试视频（ffmpeg 生成的测试图案），让抽帧链路**不依赖网络与模型**即可回归。
- 全量测试 **308 项**全绿（新增 21 项）。

### 注意

`imageio-ffmpeg` 自带 ffmpeg 二进制，**安装后 `.venv` 体积增加约 84 MB**。
这是视频识别的必要代价——没有解码器就无法从视频取画面。

---

## [1.10.3] — 2026-09-11

**确认并修复"token 超出限制"这条真凶**。

### 实测证据（12 张真实实拍图，覆盖 12 个品类）

| 指标 | 实测值 |
| --- | --- |
| 思考 token | 最少 264｜**最多 1199**｜平均 680 |
| 输出 token | **最多 1429**（上限 2000） |
| 撞上限次数 | 0 / 12 |
| 输入 token | 最多 1183（11MB 与 0.2MB 图片都是 ~1170，服务端自行压缩） |

结论：**输入侧没有风险**，但**输出侧只剩不到 40% 余量**。
这个模型是推理模型，思考也算输出 token——画面更复杂、带标牌文字时很容易越过 2,000，
一旦越界就是 `status=incomplete` + 正文为空 → 退回基础描述 → **入不了库**，
正是"视觉识别未完成，不能入库"的成因之一。

### 修复

- **默认描述输出上限 2,000 → 4,000**（`PULSE_VISION_MAX_TOKENS`）。
  注意这是**上限而非收费项**，调大不会增加正常调用的成本。
- **被截断时自动放大预算重试一次**：新增 `OutputTruncatedError` 与
  `escalated_budget()`（`max(上限×3, 上限+4000)`，封顶 16,000）。
  识别与短文生成两条链路都接上了——不再"一截断就退回"。
- `.env.example` 补上实测数据与说明。

### 验证

- 全量测试 **287 项**全绿（新增 5 项：截断异常类型、预算放大算法、
  截断后重试成功、二次仍截断则如实报错、默认上限余量）。

---

## [1.10.2] — 2026-09-11

**"视觉识别未完成，不能入库"给出真因** + 消除两个诱因。

### 问题

控制台报"视觉识别未完成，不能入库。请检查 .env 里的视觉模型配置，然后重新选图。"
——但**这句话把原因写死了**。实际可能是 Key 失效、超时、网络抖动、响应被截断，
只有第一种情况才该去看 .env。真因被埋在下面一行灰色小字里，很容易漏掉。

### 修复

- **失败时回传真实原因**：`/api/describe` 新增 `vision_error` 字段，
  页面把它直接写进门禁提示（`原因：<真实错误>`），不再笼统指 .env。
  没配 Key 时给出明确措辞"未配置 PULSE_VISION_API_KEY（见仓库根 .env）"。
- **新增"视觉模型自检"按钮**（上传区顶部，`POST /api/vision-check`）：
  一键真实调用一次模型，直接告诉你是哪一阶段不行——分 `config`（没配 Key）、
  `model`（模型不能识图）、`request`（鉴权 / 网络 / 截断）三种，并附服务端原文与处理建议，
  同时显示当前模型、接口地址、超时、Key 是否已配置。不用再翻 .env 猜。

### 消除诱因

- **网络抖动只重试一次**（超时 / 连接中断 / 网关 5xx / 429）：
  "大图超时"会让识别直接退回基础描述、进而入不了库，重试一次能挡掉多数偶发。
  鉴权类错误（401）不重试——Key 错了重试也没用。
- **默认超时 45s → 90s**：11MB 实拍图实测就要 13 秒，45 秒对大图偏紧。
  `.env.example` 已同步；已把本机 `.env` 的 `PULSE_VISION_TIMEOUT` 从 45 调到 90。

### 排查结论

用 5 张真实图片（含 11MB）跑通识别链路，**全部成功并拿到入库凭据**；
走真实 HTTP multipart 上传同样正常。自检也确认当前 `.env` **可用**。
所以那次失败不是配置问题，更可能是网络偶发——现在重试 + 自检按钮都能覆盖。

### 验证

- 自检接口三种情况实测：正常 → "可用"；错 Key → `HTTP 401 Invalid API key`；
  无 Key → "未配置 PULSE_VISION_API_KEY"。
- 全量测试 **282 项**全绿（新增 11 项，含重试行为与错误透传）。

---

## [1.10.1] — 2026-09-11

**修复禁用态按钮文字读不出来**（导出按钮"看不见"的根因）。

### 修复

- **禁用态按钮的标签与背景对比度只有 1.42:1**（可读性要求 ≥ 4.5:1），
  近乎白字配浅灰底——按钮看着就是个灰块，标签等于不存在。
  受影响的是两个**默认处于禁用态**的按钮：

  | 按钮 | 位置 | 何时可点 |
  | --- | --- | --- |
  | 上传并入库 | 上传区 | 视觉识别成功后 |
  | 导出这一帖的素材 | 召回区 | 执行召回后 |

  改为深灰字配浅灰底（**4.82:1**），禁用态依然可读。
  这是 1.5.0 给"上传并入库"加禁用样式时引入的，1.10.0 的导出按钮继承了同一个问题。
- **导出按钮位置下移**：原先排在召回结果**上方**，4 个位次的长图一出，
  按钮就被挤到看不见的地方。现移到结果**下方**，与"复制导出文件夹路径"并排成一条操作栏，
  视觉流程变成"召回结果 → 导出 → 生成短文"。
- 提示文案订正：原写"先在**下面**执行召回"，而召回表单其实在上面。

### 验证

- 新增 2 项**对比度守护测试**：直接解析页面 CSS 里的颜色并计算 WCAG 对比度，
  低于 4.5:1 即失败——这类"样式把文字洗掉"的问题以后会被测试挡住。
- 真实页面 JS 在最小 DOM 沙箱里跑通导出按钮全流程 **9/9**：
  召回前禁用、召回后可点、点击调用 `/api/export`、载荷带平台与位次、
  导出后显示落盘目录与复制路径按钮。
- 全量测试 **271 项**全绿。

---

## [1.10.0] — 2026-09-11

**召回结果加导出功能**（媒体库契约 v1.2 → v1.3）。

### 新增

- **素材包导出**：召回结果可一键导出成文件夹，落在素材库旁边的
  `菲美得产品图片/../Pulse导出/<平台名>_<时间>/`（可用 `PULSE_EXPORT_DIR` 改）。
  文件夹里是：

  | 内容 | 说明 |
  | --- | --- |
  | `01_…`、`02_…` | 按**位次顺序**复制的素材原图，视频一并带上，文件名带品类 |
  | `说明.txt` | 每个位次要什么画面、实际用哪张、画面描述、报告口径出处 |
  | `文案.txt` | 有短文时写入：正文 + 标签 + 第一条评论 + 英语母版 + 发布前自查 |

- **零修饰**：只做字节级复制（`shutil.copy2`），不裁不压不调色——报告 §0.1 取消了全部后期加工。
- **不覆盖**：重名目录自动加序号，绝不静默盖掉上一次导出。
- **所见即所得**：导出用的是页面**当前显示**的位次与素材（由页面原样回传）——
  导出时重跑召回会因加权随机挑出别的图。
- **缺失如实报告**：素材文件不在磁盘上时计入 `missing`、写进 `说明.txt`，
  界面显示"有 N 条素材没找到"，不静默少导。

### 验证

- 真实图库端到端：LinkedIn 导出 4 张共 9.6 MB，Facebook 正确带上
  **浇铸现场视频**（6.7 MB mp4）共 14.5 MB，文件名按位次排序，
  `说明.txt` 含位次用途与报告出处。
- 全量测试 **269 项**全绿（新增 18 项）。

---

## [1.9.0] — 2026-09-11

**按平台召回加上图片预览与短文生成**（媒体库契约 v1.1 → v1.2）。

### 新增

- **素材预览**：召回结果直接显示缩略图（`GET /api/asset-image?asset_id=…`），
  视频用可播放的 `<video>`。支持 `Range` 请求（206），否则视频拖不动进度。
  定位文件**不使用**描述头部的 `source_path`——那里是别的机器上的绝对路径
  （`D:/菲美得/…`）；改为从描述文件的相对路径推目录、按文件名查找，
  文件名只取 basename 且解析结果必须落在素材目录内（防路径穿越）。
- **短文生成**：`pulse/services/media/captions.py` 把报告 §2.1/§3 的文案模式固化成
  `CaptionSpec`，按平台生成并逐条校验。页面在召回结果下方给出生成面板，
  可填补充要求、生成后直接编辑复制。

| 平台 | 结构 | 长度 | 标签 | 语言 |
| --- | --- | --- | --- | --- |
| LinkedIn | 钩子行→痛点→能力证据→产品范围→CTA→标签 | 600–1,200 | 3–5 | 英语 |
| Facebook | 场景描述→一句结论→提问收尾→标签 | 150–400 | 1–2 | 英语 |
| TikTok | 钩子→一句结论→标签 | ≤150 | 4–5 | 英语 |
| VK | 企业介绍→工艺→产品→设备→合作方式 | 500–1,500 | 2–5 | **俄语**（另存英语母版） |

- **校验回显**：长度、标签数与规范、emoji 数、禁用语（报告 §8.3）、
  未核实参数（`find_unverified_claims`）、外链位置、VK 俄语、折叠区。
  校验**不阻断**生成，只把结果如实标出，由用户判断。
- **不做静默降级**：模型不可用或输出不完整时直接报错，不返回模板拼凑的文本。

### 修复

- **短文生成返回空正文**：`deepseek-v4.1-flash` 是推理模型，写一篇 LinkedIn 短文时
  思考就吃掉 4,000 tokens，把输出预算耗尽、正文一个字都没出，而错误只报
  "返回内容不是 JSON"，完全没法排查。实测后修三处：短文思考强度设为 `low`、
  输出预算单独放宽到 8,000（`PULSE_CAPTION_MAX_TOKENS`）、
  并新增 `check_response_complete` 把 `status=incomplete` 的原因直接说清楚。
- `_loads_json_object` 解析失败时带上原文片段（截断类问题一眼可辨）。
- 外链校验原先按"是否需要首条评论"判断，逻辑写反了；改为由 `link_in_body_allowed`
  决定（LinkedIn / Facebook / TikTok 正文不得有链接，VK 允许）。
- `VisionDescriber._post` 的 HTTP 调用抽成模块级 `post_json`，描述与短文共用，
  避免两处各写一份请求与报错逻辑。

### 验证

- 真实图库 + 真实模型端到端：LinkedIn 四张位次图全部预览成功（回传字节数与磁盘文件一致）；
  四个平台短文全部生成，LinkedIn 1,162 字符 / 5 标签、TikTok 132 字符 / 5 标签、
  VK 1,163 字符俄语，校验基本全绿。
  Facebook 生成 416 字符、超出 400 上限——校验如实标出「！长度」，
  这正是校验该起的作用。
- 全量测试 **251 项**全绿（新增 31 项）。

---

## [1.8.1] — 2026-09-11

**仓库整理：清除开发过程遗留文件，并让全部文档版本与计数对齐**。

### 清理

- 删除 `Pulse海外社媒Agent开发文档_v0.2.1.docx`——已被 **v0.3** 完全覆盖
  （v0.3 在其基础上新增媒体资产库与召回策略、FR-15~FR-19）。
  `AGENTS.md` §1 文档优先级已同步改指 v0.3；旧版仍可从 git 历史取回。
  这是沿用本项目既有做法（v0.2 曾在 v0.2.1 到位后被删除）。
- 删除仓库内 13 处 `__pycache__`／`.pyc` 与素材工具目录下 1 处缓存。
- 删除工作区根的两张开发期控制台预览图（非交付物）。

### 版本与计数对齐

- `README.md`：头部"当前版本"由 V1.0.0 改为 **V1.8.0**；标签/发布示例、
  分支约定由 1.0 改为当前值；素材计数订正为
  **318 份素材描述 + 13 份汇总索引**、实拍素材 341 个文件。
- `pulse/tasks/TASKS.md`：版本管理节由 V1.0.0 更新为 **V1.8.0 / 契约 1.1**；
  基线测试计数 20 → **21 项**；补记本轮整理清单。
- `pulse/docs/dispatch/A1_content.md`、`A6_verifier.md`：素材计数与基线计数对齐；
  新增"`社媒成片/` 17 张已停用图不得补描述"的说明。
- `AGENTS.md`：文档优先级清单改指 v0.3；素材文件数订正。
- `.gitignore`：注释中的素材计数订正。

### 记录

- 在 `README.md` §5 与 `TASKS.md` 版本管理节写明**版本漂移教训**：
  1.4.1–1.7.1 期间只打 git tag、未同步 `pyproject.toml` / `pulse.__version__` /
  `CHANGELOG.md`，而守护测试看不到 tag，导致漂移未被发现。
  发布时**四处必须同时改**。

### 验证

- 全量测试 **220 项**全绿；`pulse/__version__` 与契约版本输出 `1.8.1 1.1`。
- 核对项：仓库内无 `__pycache__`／`.pytest_cache`；`git status` 除本次改动外干净。

---

## [1.8.0] — 2026-09-11

**契约 v1.1：VK 转正式运营**，并补齐 1.4.1–1.7.1 的版本元数据与变更记录。

### 契约变更（v1.0 → v1.1）

- **VK 由"冻结"转为正式运营（P1）**。依据 VK 法务评审通过的结论
  （《菲美得_四平台推荐风格与方式报告_v1》§8.1，2026-09-11），用户同日确认转正。
- `Platform` 枚举新增 `VK = "vk"`；TikTok 维持"暂不投入"，**仍不入枚举**。
- 新增 VK 必填项 `options.owner_id`（**必须为负数的社区 ID**，正数会发到个人墙，已拦截）
  与可选 `from_group`（布尔）。
- 平台优先级更新为：P0-A = LinkedIn + YouTube；**P1 = Reddit + Facebook + VK**；
  P2 = Instagram；暂不投入 = TikTok。
- **附带留痕义务**（写进 AGENTS.md §3.4）：每季度制裁名单筛查、新询盘出口对象筛查、
  俄语审校记录、VK 独立素材台账。法务通过 ≠ 从此不用管。
- VK 是唯一非英语平台，对外文案为俄语、由英语母版翻译派生。
  契约中已注明该口径，但**未**在代码层强制校验（`Caption` 目前只有 `text` / `text_zh`）。

### 修复

- **版本元数据漂移**：`pyproject.toml` / `pulse.__version__` / `CHANGELOG.md`
  此前停留在 1.4.0，而 git tag 已到 v1.7.1。本次一次性追平到 1.8.0，
  并补写 1.4.1–1.7.1 的变更记录。

### 验证

- 契约基线 **21 项**全绿（`test_02` 由"排除 VK"改为"包含 VK、排除 TikTok"，
  新增 `test_02b` 校验 VK 社区 ID 规则）。
- 全量测试 **220 项**全绿。

---

## [1.7.1] — 2026-09-11

**区分"位次为空"是没素材还是在冷却期**。

### 修复

- 按平台召回时，某个位次挑不到图可能是两种情况：真的没有这种画面，或者有但都在
  15 天冷却期内。原来一律提示"建议补拍"，会让人白跑一趟。现在接口返回
  `blocked_by_cooldown`，两种情况的页面提示分开。

### 验证

- 新增 2 项测试；全量 219 项通过。

---

## [1.7.0] — 2026-09-11

**召回改为按平台下拉选择**。

### 新增

- `pulse/services/media/platforms.py`：把《四平台推荐风格与方式报告》§3 里
  "每个平台该配哪些画面、几张、什么顺序"变成可执行数据。四个平台各一组**画面位次**。
- 召回支持 `platform` 参数，按位次逐格挑图；位次挑不到就留空并提示补拍，
  不拿别的图凑数。页面回选区改为"目标平台下拉 + 每格条数 + 可选关键词"。

### 修复

- **汇总索引文件被当成图片素材**：原判定只比对固定文件名 `00_汇总索引.md`，
  而库里存在 `00_发泡工段汇总索引.md` 这类异名索引，会被召回并虚增容量统计。
  现改为按 `00_` 前缀 + 头部 `content_type: 汇总索引` 双重判定。

### 验证

- 新增 18 项测试；全量 218 项通过。

---

## [1.6.0] — 2026-09-11

**品类改由图片自动判定，修复上传表单列宽错位**。

### 新增

- `pulse/services/media/categories.py`：品类清单由目录扫描得出（不硬编码）；
  品类判定优先级为"模型判定 → 关键词兜底 → 调用方已选 → 留空待人工"。
- 识别时把既有品类清单喂给视觉模型，要求只能从清单里选；控制台的品类/子类改为下拉框。

### 修复

- **上传表单列宽错位**：原布局用 `grid-template-columns: repeat(auto-fit, minmax(180px,1fr))`，
  列数不确定导致列宽忽宽忽窄、标签基线对不齐。改为四列固定网格 + `minmax(0,1fr)`，
  标签统一 20px 行高、输入框统一 36px 高。
- 目录名与描述头部不一致的问题（如 `人员/会议与培训`、`发泡（白模成型）`）：
  位次匹配改为只锁定顶层品类，细分靠关键词。

### 验证

- 新增 25 项测试；全量 201 项通过。前端逻辑经最小 DOM 沙箱 17/17 验证。

---

## [1.5.0] — 2026-09-11

**入库许可：必须先完成视觉识别才允许入库**。

### 新增

- 识别成功签发入库凭据 `vision_ticket`，绑定图片内容哈希、有效期 1 小时；
  入库无凭据一律 **403**。前端按钮在识别成功前为灰色不可点，
  **服务端同样校验**，绕过页面直接调接口也进不来。
- 应急开关 `PULSE_MEDIA_REQUIRE_VISION=0`（仅限视觉服务长时间故障时使用）。

### 验证

- 真实图片端到端：未识别入库 → 403；识别后带凭据 → 200。
  前端状态机经 DOM 沙箱 13/13 验证；全量 176 项通过。

---

## [1.4.1] — 2026-09-11

**修复视觉识别 400**。

### 修复

- **根因**：OpenCode Go 强制要求 `x-opencode-session` 请求头，缺失时任何请求都返回
  `400 MissingSessionID`。与密钥、模型、请求体格式均无关。
- 请求自动附带会话头（可用 `PULSE_VISION_SESSION` 覆盖）。
- **报错不再只说 "400 Bad Request"**：新增 `VisionAPIError`，把服务端错误类型、
  原文与中文处理建议一并抛出。
- 默认模型改为用户指定的 `deepseek-v4.1-flash`（实测可识图）。

### 验证

- 自检显示"可用"；真实图片端到端 `source=vision`；全量 167 项通过。

---

## [1.4.0] — 2026-09-11

**视觉模型接入改为 OpenCode Go + DeepSeek，并新增配置自检**。

### 变更

- **接入形态**：OpenCode Go 使用 Responses API（`https://opencode.ai/zen/go/v1/responses`），
  描述器同时支持 `responses` 与 `chat`（`/chat/completions`）两种形态，用
  `PULSE_VISION_API_STYLE` 切换，默认 `responses`。
- **默认模型**：`deepseek-v4-flash-vision-exp`。模型目录显示
  `deepseek-v4-flash` / `deepseek-v4-pro` / `deepseek-flash` **只支持文本输入**，
  拿它们识图会直接失败，因此配置成这三个模型时会在调用前拦截并给出中文提示。
- **配置自检**：新增 `python -m pulse.console.launcher --check-vision` 与
  `检查视觉模型.bat`，用一张 8×8 探针图验证"地址 / 密钥 / 模型能否识图"，
  失败时区分 config（未填 Key）、model（模型不支持图片）、request（网络或鉴权）三个阶段。
- **随仓库提供空配置**：`.env`（已 gitignore，Key 留空待填）与 `.env.example`（含说明）。

### 验证

- 未填 Key：自检返回"未配置 PULSE_VISION_API_KEY"。
- 用无效 Key 实测：接口返回 `401 Unauthorized`（`/zen/go/v1/responses`），
  证明地址与请求形态正确，只差有效 Key。
- 测试基线：**161 项全绿**（1.3.0 的 156 项 + 本次 5 项）。

---

## [1.3.0] — 2026-09-11

**素材描述自动生成：上传图片不再需要手填描述**。

### 新增

- **描述生成模块** `pulse/services/media/describe.py`：
  - 视觉模型路径：调用 OpenAI 兼容的多模态接口识别画面（配置 `PULSE_VISION_API_KEY`
    即启用），提示词强制"只描述肉眼可见内容，禁止推测材质 / 公差 / 单重 / 产能"；
  - 基础信息兜底：未配置模型时，用文件名、拍摄时间、分辨率、品类等**可见事实**
    自动拼装描述，并明确提示"画面内容待补充"；
  - 参数防编造：`find_unverified_claims` 检出模型输出的牌号 / 吨位 / 公差 / 产能等
    画面不可见参数，作为警告返回人工核对（AGENTS.md §3.7）。
- **控制台交互改造**：上传表单不再要求手填摘要 / 关键词 / 细节；选择图片或切换品类即调用
  `POST /api/describe` 自动生成并回填（仍可直接修改）；入库接口在描述为空时自动生成。
- **配置样例** `.env.example`：视觉模型地址 / 密钥 / 模型名 / 超时。
- **测试** 13 项（`pulse/services/media/tests/test_describe.py` 9 项 + 控制台 4 项）。

### 说明

- 未配置视觉模型不报错：自动降级为"基础信息描述"；模型调用失败也会降级并在页面提示原因。
- 图片尺寸解析用标准库完成（PNG / JPEG / GIF / BMP / WebP），不新增第三方依赖。
- 测试基线：**156 项全绿**（1.2.0 的 143 项 + 本次 13 项）。

---

## [1.2.0] — 2026-09-11

**一键启动：给非技术同事的素材库控制台入口**。

### 新增

- **双击启动**：仓库根目录 `一键启动_素材库控制台.bat`——自动进入仓库目录、
  调用 `.venv` 解释器、挑选空闲端口、自动打开浏览器，并用中文提示启动状态与容量预警。
- **一键建快捷方式**：`创建桌面快捷方式.bat`——在桌面生成「Pulse素材库」图标，
  以后从桌面双击即可。
- **启动器模块** `pulse/console/launcher.py`：路径解析（缺目录时给中文提示，不抛栈）、
  端口挑选（8765 被占用时自动换端口）、地址构造、浏览器自动打开；
  `python -m pulse.console.launcher` 也可直接调用。
- **页面「三步上手」引导**：控制台首页顶部说明上传、看预警、标记使用三个动作。
- **测试** 10 项（`pulse/console/tests/test_launcher.py`）。

### 说明

- 台账文件默认写到 `.pulse/media_ledger.sqlite3`，已加入 `.gitignore`，不会污染仓库。
- `.bat` / `.cmd` 在 `.gitattributes` 中固定 CRLF，避免 cmd 解析异常。
- 测试基线：**143 项全绿**（1.1.0 的 133 项 + 本次 10 项）。

---

## [1.1.0] — 2026-09-11

**素材库入库与召回策略（新增媒体资产域 + 图形化控制台）**。

### 新增

- **媒体资产库域** `pulse/services/media/`：
  - 素材目录扫描与描述解析：`catalog.py`
  - 使用台账（sqlite，幂等）：`ledger.py`
  - 召回策略（15 天冷却 + 新图加权 + 加权随机采样 + 容量预警）：`policy.py`
  - 实拍图入库（写描述 MD、重建汇总索引、内容哈希查重）：`ingest.py`
  - 新增素材登记表：`registry.py`、策略配置：`config.py`
- **图形化素材库控制台** `pulse/console/`：纯标准库 HTTP 服务 + 内嵌页面，
  支持上传入库、容量红色预警、冷却状态查看、模拟召回与"标记已用于内容"。
- **素材库契约** `pulse/contracts/MEDIA_LIBRARY.md`（媒体域接口与业务规则）。
- **测试** 32 项（`pulse/services/media/tests/` 26 项 + `pulse/console/tests/` 6 项）。

### 业务决策（2026-09-11 确认）

- **冷却触发口径**：只有素材**实际进入生成内容 / 发布**才计入冷却；检索命中不计。
- **品牌级素材池**：素材池按品牌隔离，当前品牌 = `沧州菲美得`。
- **红色预警阈值**：可召回容量低于 **112 张**（约一周用量）即红色预警。
- **存量口径**：既有图库全部视为**老图**，不论此前是否被使用过，不享受新图加权。
- 既有图库无入库时间，故新增素材登记表 `_媒体登记表.json` 区分新图 / 老图。

### 说明

- 测试基线：**132 项全绿**（1.0.0 的 100 项 + 本次 32 项）。
- 工业件铁律（AGENTS.md §3.6）在入库层强制执行：仅接受实拍图，不接受文生图产品图。

---

## [1.0.0] — 2026-09-10

首个冻结版本：**接口契约冻结 + 发布网关域交付**。

### 新增

- **工程接口契约 v1.0（冻结）**：`pulse/contracts/INTERFACES.md` 定义统一发布对象 `UnifiedPost`、
  平台白名单（VK / TikTok 不得进入枚举）、素材 `license_status` 规则、`ErrorClass` 三分法
  （可重试 / 不可重试 / 轮询）、ID 前缀与时间前缀规范、`PublishResult` "受理 ≠ 发布" 语义。
- **跨域共用类型**：`pulse/shared/`（`models`、`enums`、`ids`、`__init__`）与 20 项契约基线测试。
- **发布网关域（A2）**：`pulse/services/publish/`
  - 网关主流程与幂等键透传：`gateway.py`
  - 状态机与本地存储：`state.py`、`store.py`
  - 平台适配器：LinkedIn、YouTube，以及测试用 `fake.py`；统一基类与错误模型：
    `base.py`、`errors.py`
  - **半自动兜底**（P0 能力，非降级方案）：`semi_auto.py`
  - 传输层：`transport.py`
  - 域内单元测试：适配器、契约形状、网关、待定终结、半自动（共 80 项）
- **多 Agent 协同治理基线**：`AGENTS.md`（目录所有权与铁律）、`pulse/docs/多Agent协同开发方案.md`
  （分工原理与波次设计）、`pulse/docs/dispatch/`（A1–A6 派工单）、`pulse/tasks/TASKS.md`（任务板）。
- **版本管理与持续集成**：`.gitattributes`（统一 LF）、`.github/workflows/ci.yml`
  （`main` 推送与 PR 跑全量 pytest，`v*` 标签自动创建 Release）。

### 说明

- 测试基线：**100 项全绿**（`pulse/shared/tests` 20 项 + 发布网关域 80 项），
  运行方式 `python -m pytest -p no:cacheprovider`。
- 平台优先级：P0-A = LinkedIn + YouTube；P1 = Reddit + Facebook；P2 = Instagram；
  **VK 冻结**（合规红线）；TikTok 暂不投入。
- 业务数据（`菲美得产品图片/`、`外贸公司/`、`RAG知识库/`）属项目数据，不入代码库。

### 待办（不在 1.0.0 范围内）

- A1 内容生产域、A3 调度与账号域仍在并行开发中，交付后进入 1.1.0。
- A4 合规 / A5 平台前端 / A6 独立验证于 W2 波次启动。
