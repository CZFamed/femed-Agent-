# 视觉模型配置（可选）：填好 API Key 后，控制台上传图片会自动识别画面内容并生成描述。
# 未配置时仍可用，控制台会退化为"文件名 + 拍摄时间 + 分辨率"的基础信息描述。
#
# 用法：复制本文件为 .env，填入 key（.env 不会入库）。

PULSE_VISION_BASE_URL=https://api.openai.com/v1
PULSE_VISION_API_KEY=
PULSE_VISION_MODEL=gpt-4o-mini
PULSE_VISION_TIMEOUT=45
