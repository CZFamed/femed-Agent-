# Pulse 海外社媒 Agent —— 需求拆解（基于《Pulse海外社媒Agent开发文档_v0.2.1》）

> 拆解日期：2026-09-10　｜　源文档：`Pulse海外社媒Agent开发文档_v0.2.1.docx`（v0.2.1，2026-09-10）
> 性质：需求分析（不改变原文结论，只做条目化、补全隐含需求、标注缺口）

---

## 0. 一句话结论

这是一份**已经收敛得相当彻底的技术设计型需求文档**：平台范围已定版（TikTok / LinkedIn / YouTube / Facebook / VK 五平台主清单），技术栈已定版（Python + LangGraph + Celery/Redis + PostgreSQL），发布链路已按"官方 API 优先 + 半自动兜底"设计完毕。

真正没定的不是技术，而是**三类业务前提**：首发地区与时区、单条内容成本预算、以及真实业务场景与文档示例场景之间的落差。

拆解后可落到 4 条工程主线：**① 生成与合规 ② 审批与调度 ③ 发布网关（含配额与幂等）④ 账号与凭据中心**，外加一条贯穿始终的**半自动兜底链路**。

---

## 1. 文档画像：它是什么，不是什么

| 维度 | 判断 |
| --- | --- |
| 文档类型 | 混合版：需求规格 + 技术设计（决策已做，非纯需求收集） |
| 成熟度 | 高。已含验收标准、错误分级、状态机边界、数据表结构、接口摘要 |
| 已定版决策 | 平台主清单、技术栈、队列选型、审核状态机、发布回执口径 |
| 尚未定版 | 文生图/TTS 厂商、渲染层选型、Best time 初始数据、首发音种与地区 |
| 显著缺口 | **运营控制台（UI）本身没有被列为需求条目**，但 FR-4 审批工作流是 P0 |

一句话：**后端链路设计得比前端入口清楚**。FR-4"一键通过"如果落地为 API，运营是没法用的。

---

## 2. 需求全景图

```
输入层        内容生产            质量闸门          排队与发布          结果
─────────  ──────────────  ──────────────  ───────────────  ──────────────
brief  ──►  FR-1 文案生成  ──►  FR-5 合规预检  ──►  FR-6 排期发布  ──►  FR-8 数据回捞(P1)
主题        FR-2 媒体素材      FR-4 审批流        · 配额预检           FR-9 看板(P1)
卖点        FR-3 Variant派生   · block/warn       · 幂等去重
人群                           · AI 披露          · 错误分级重试
                                                  · 异步回执收敛
        ─────────────────────────────────────────────────────────────
        横切：FR-7 账号与凭据中心 │ 合规风控（§6）│ 审计与监控（§8）
```

---

## 3. 需求条目化拆解

### 3.1 使用角色（4 类）

| 角色 | 核心诉求 | 对应功能 |
| --- | --- | --- |
| 内容运营 | 批量出稿 / 审稿 / 改稿，省跨平台生产时间 | FR-1/2/3/4 |
| 品牌·市场负责人 | 审批、品牌一致性、看板 | FR-4/9 |
| 出海电商团队 | 围绕商品与活动保持稳定更新 | FR-1/2/6 |
| 多账号操盘手 | 多品牌多账号统一调度（后期） | FR-10 |

### 3.2 核心场景（US-1 ~ US-6）

| 场景 | 内容 | 关键约束 |
| --- | --- | --- |
| US-1 | 一条内容多平台分发 | 各平台篇幅/标签/CTA 均不同；Instagram 不进 MVP 生成清单 |
| US-2 | 品牌化生成 | 受 Brand Guide 约束，同指令输出稳定 |
| US-3 | 审批后发布 | 未通过的内容**不得**进入发布池 |
| US-4 | 定时发布 | 目标时区最佳时间；支持手动指定；账号可随时暂停 |
| US-5 | 合规预检 | 版权/违规链接/垃圾特征/敏感词/AI 披露 |
| US-6 | 发布回执 | 失败分级重试+告警；M2 起回捞互动数据 |

### 3.3 功能需求（FR-1 ~ FR-11）

优先级：**P0** = MVP 必须　**P1** = 二期（M2/M3）　**P2** = 远期

| 编号 | 功能 | 优先级 | 验收标准（原文） |
| --- | --- | --- | --- |
| FR-1 | 内容生成（文案） | P0 | 含主文案/话题标签/CTA/配图建议；支持重生成与换语气；同 brief 多版本可对比 |
| FR-2 | 媒体素材生成 | P0 | 支持 9:16、1:1、16:9、4:5；仅授权来源；自动成片 M2 起 |
| FR-3 | Variant 派生 | P0 | 同一 source_id 可追溯全部 variant 及状态 |
| FR-4 | 审批工作流 | P0 | 未通过不进发布池；审批留痕（人/时间/diff） |
| FR-5 | 合规预检 | P0 | 拦截项给出原因与位置；可人工豁免并留痕；block 需管理员权限 |
| FR-6 | 排期发布 | P0 | 成功率可监控；错误分级可见；异步回执有明确终态；重复投递不重复发布 |
| FR-7 | 账号与凭据中心 | P0 | 凭据加密且业务层不可见；账号可停用并熔断发布 |
| FR-8 | 数据回捞 | P1 | 数据落库，可按内容/平台/时间聚合（M2 交付） |
| FR-9 | 发布看板 | P1 | 成功率低于阈值告警；指标口径可配置 |
| FR-10 | 矩阵与多账号调度 | P1 | 单账号失败不影响其他账号；防封信号自动暂停告警 |
| FR-11 | A/B 草稿与历史重发 | P2 | 可对比同主题不同变体效果 |

#### P0 需求的工程拆解（文档未展开、但必须实现的部分）

**FR-1 内容生成**，可拆为 6 个可独立验收的子任务：

1. Brief 接收与解析（结构见 §4.1，含 `extend_platforms`、`budget_media`、`token_budget`）
2. 品牌语调注入（Brand Guide 加载）
3. 平台 Prompt 模板渲染（每平台独立约束：字数 / emoji / 标签数 / 结构）
4. 话题标签组合（品牌词+品类词+地区词+当季热点，白名单防跑偏）
5. 多语言输出（按目标地区语言，保留中英双语稿供审校）
6. "生成 → 自评 → 重排"轻量反馈（N_candidates=1–2，自评仅一轮，**不做无限采样**）

**FR-4 审批工作流**，状态机是硬边界，必须逐态实现：

```
draft → pending_review → approved / rejected / needs_revision
approved → scheduled → publishing → published / failed
scheduled → cancelled（人工暂停，不串台）
publishing → pending_finalize（平台异步处理中，轮询或回调收敛）
failed → 限流/瞬时错误 → 自动重试 → publishing
       → 认证失效 → 刷新 Token 重试
       → 政策拒绝 → 不重试，转 rejected + 告警
```

强制单条复核的 3 个触发条件（**不可走"整批通过"**）：首次接入某账号、命中合规告警、更换 Brand Guide。

**FR-6 排期发布**，是全文风险最密集的模块，拆为 7 项：

1. 延迟任务（delayed job，**不用 cron 硬排**，便于取消/重排）
2. 每账号"令牌桶限流 + 发布冷却 + 平台滚动窗口额度"
3. **发布前配额预检**（TikTok / VK / IG 对额度敏感）
4. 幂等：`unified_post_id` 透传；不能传的平台用"发布前查重 + 本地状态机"兜底
5. 异步回执：上传后先进 `pending_finalize`，**避免把"已受理"误判为"已发布"**
6. 错误分级重试（5 类：限流 / 5xx / 认证失效 / 素材处理中 / 政策拒绝）
7. 账号停用即时挂起该账号全部任务，队列消息熔断不投递

### 3.4 非功能需求（散落在 §6 / §8，需归纳成条目）

| 类别 | 要求 |
| --- | --- |
| 安全 | Token 仅经账号中心签发，Vault/KMS 加密落库，**永不落日志**，业务层不可见明文 |
| 隔离 | 素材 / brief / 品牌资料按账号·租户隔离（多租户模型） |
| 审计 | 审批、发布、凭据操作等敏感动作全量留痕 |
| 可用性 | 净发布成功率 ≥ 98%，跌破 95% 告警；单账号连续限流即暂停 |
| 运维 | 调度器支持一键暂停全部定时任务；适配器可单平台回退半自动 |
| 测试 | 关键函数覆盖率 ≥ 80%；换模型/换提示词必须做生成质量回归 |
| 合规红线 | 只用官方 API，不做脚本模拟人工发帖、不绕风控、不承诺"零封号" |
| 提醒 | 凭据到期前 7 天提醒；token 超预算熔断告警 |

---

## 4. 平台接入约束（决定"全自动 vs 半自动"的核心表）

这张表决定产品形态，而不是技术选型。**能全自动的平台只有 4 个，且都带前置审批。**

| 平台 | 本版接入方式 | 前置审批（关键路径） | 硬性限额 |
| --- | --- | --- | --- |
| LinkedIn | 公司页全自动；个人号"生成后一键复制"半自动 | 开发者应用 + 组织 API 权限（企业身份） | 个人动态 API 常遇 403 |
| YouTube | 配额内自动上传 | API 服务启用；超默认配额需合规审计 | **1,600 单位/次上传，默认 10,000/天 ≈ 6 条/天** |
| Facebook | 主页自动/排期；群组半自动 | Meta App Review（Pages） | Groups 无成熟公开发文 API |
| TikTok | MVP 生成成品 + 一键跳转；M3 切自动 | 企业认证 + Direct Post 审计 | 审计周期常见 **2–6 周** |
| VK | 主清单；授权后自动发社区（M2），未授权走半自动 | VK 应用 + wall/manage 权限 | 需评估当地内容与数据法规 |
| Instagram（P1） | 先半自动 | `instagram_content_publish` + App Review；须绑定 FB 主页 | 滚动窗口约 **100 条/24h/账号** |
| Reddit（P1） | 人工确认子版块后发布 | Responsible Builder 商用申请（**需书面授权且计费**） | 自我推广约 1:10 惯例 |

**通用兜底**：任一平台都保留半自动模式 —— 生成好内容 + 控制台"复制 / 唤起官方 App"。
> 工程含义：**半自动链路是一条独立的 P0 需求**，不是降级方案。MVP 阶段它承担了 TikTok 与 Facebook 群组的全部发布量，文档却没有为它编号。

---

## 5. 架构与技术拆解

### 5.1 分层结构

```
[接入] API Gateway（鉴权/限流/审计）
[编排] Agent Orchestrator —— 生成→合规→审批→排期→发布，可恢复
[服务] 内容生成 │ 合规检测 │ 审核 │ 调度器
[队列] 消息队列（延迟任务/重试/配额预检）
[发布] 发布网关 ├─ TikTok ├─ LinkedIn ├─ YouTube ├─ Facebook ├─ VK
                ├─ Instagram(P1) └─ Reddit(P1 预留)
[外部] 各平台 Open API
[横切] 账号与凭据中心 │ 业务库/对象存储/向量库 │ 监控告警与审计
```

设计要点：调度器**不直连平台**，只投递队列消息，由网关异步消费 —— 便于削峰、限流、重试、按账号熔断。

### 5.2 技术栈（已定版 + 待定）

| 组件 | 选型 | 状态 |
| --- | --- | --- |
| 应用框架 | Python 3.x | 定版 |
| LLM | GPT 系列，**模型路由**（贵模型写长文 / 轻模型改写自评） | 定版 |
| Agent 编排 | LangGraph（多步 + 人工中断 + 可恢复状态） | 定版 |
| 任务队列 | Celery + Redis | 定版 |
| 数据存储 | PostgreSQL + Redis + 对象存储 + 向量库 | 定版 |
| 凭据 | Vault 或 KMS | 定版 |
| 文生图 / TTS | GPT Image / Flux / Midjourney；ElevenLabs / Azure / 火山 | **待选型评审** |
| 渲染层 | FFmpeg / **Remotion（M2 主选建议）** / 托管 API / DaVinci | **待定版** |

### 5.3 数据模型（8 张核心表）

`accounts` → `credentials`（access/refresh 双生命周期 + 吊销状态）
`contents(source)` → `variants`（platform + status + fields.jsonb + ai_score）
`media_assets`（source + license_status 支撑版权审计）
`approvals`（actor + action + diff）
`schedules`（**显式存 timezone**，便于多时区聚合）
`publish_jobs`（status 含 publishing / pending_finalize + attempts + next_retry_at）
`publish_results`（platform_post_id + post_url + metrics.jsonb）

> 待补：`brand_guides` 表在 `contents.brand_guide_id` 中被引用，但**表结构未定义**。

### 5.4 对外接口（9 个端点，P0）

`POST /briefs` · `GET /contents/{id}/variants` · `PATCH /variants/{id}/status` · `POST /variants/{id}/schedule` · `PATCH /schedules/{id}` · `POST /schedules/{id}/publish` · `GET /accounts` · `POST /accounts/{id}/oauth` · `DELETE /accounts/{id}/credential`

---

## 6. 交付节奏与关键路径

| 阶段 | 范围 | 成功标准 |
| --- | --- | --- |
| **M0 准备**（并行启动） | 开发者账号与 API 审批、文生图/TTS 选型、素材授权库、Best time 初始表 | 关键审批全部提交；选型定版 |
| **M1 内容生成 MVP** | 5 平台文案生成 + 审核 + **半自动发布** + 可编辑工程（分镜 JSON） | 单账号单平台跑通"生成→审核→发布→回执"闭环 |
| **M2 全自动试点** | LinkedIn/YouTube/Facebook(主页)/VK(社区) 自动发布 + 调度 + 幂等 + 异步回执 + 模板化自动成片 + 数据回捞 | 试点账号稳定运行 2 周：无封号、无失败积压、回捞可聚合 |
| **M3 扩展** | TikTok Content Posting 接入、IG/Reddit P1 试点、多模态自评与 A/B 剪辑、发布看板 | 看板可复盘 |
| **M4 增强** | 矩阵多账号、防封自适应、A/B 草稿 | 多账号并行稳定 |

**关键路径判断**：M0 的 API 审批（尤其 TikTok 2–6 周、Meta App Review）与开发并行，但**审批不通过 = 该平台 MVP 只能半自动**。因此 M0 应在本周启动，且不应等开发排期。

短视频能力分三阶段：**M1** 分镜 JSON + TTS + 可编辑工程（剪映/PR 二次加工，JSON schema 在本阶段定版）→ **M2** 模板化自动成片（词级字幕/配乐/卡点，单条≤3 变体）→ **M3** 多模态自评 + A/B 变体。

---

## 7. 风险与开放问题

### 7.1 风险表（原文 6 项，负责人全部空缺）

| 风险 | 等级 | 缓解措施 |
| --- | --- | --- |
| 平台 API 收紧 / 封禁 | 高 | 半自动兜底 + 网关单平台切换 |
| 账号被判垃圾 / 封号 | 中 | 真人频率、配额预检、熔断、合规预检 |
| AI 内容被平台降权 | 中 | 控机器味、质量回归、AI 披露合规 |
| LLM 成本超预算 | 中 | token 上限熔断、模型路由、候选数限制 |
| 内容不合品牌 / 法规 | 中 | Brand Guide + 词库 + 法务参与 |
| 平台异步回执丢失 / 超时 | 中 | pending_finalize 轮询 + 回调 + 超时告警 |

### 7.2 必须评审确认的 6 个开放问题

1. **首发地区与时区集合？**（决定 Best time 初始表 + 合规词库）
2. **单条内容成本预算目标？**（决定候选数与 token 上限）
3. MVP 主清单是否包含 Facebook 主页自动发布？（本版建议：包含）
4. IG / Reddit 是否作为 P1 试点推进？（本版建议：均为 P1，先半自动）
5. 文生图 / TTS 最终厂商选型？
6. **VK 纳入主清单后，首发地区是否包含俄语/独联体市场？** 语言、素材与合规资源是否到位？

> 问题 1、2、6 是**阻塞性**的：不定这三项，Best time 表、token 预算、词库与 TTS 语种都无法开工。

---

## 8. 需求缺口清单（拆解后新增，原文未覆盖）

以下 12 项在原文中没有独立条目，但按现有需求推导必然要交付，建议补编号：

| # | 缺口 | 为什么必须补 |
| --- | --- | --- |
| G1 | **运营控制台（UI）需求** | FR-4 是 P0，但"一键通过/修改/整批通过"没有界面无法交付；文中"控制台"出现 2 次却无需求条目 |
| G2 | **半自动发布链路** | TikTok、FB 群组在 MVP 全靠它；"复制/唤起官方 App"需独立编号与验收 |
| G3 | **Brand Guide 数据模型与管理** | `brand_guide_id` 被引用但无表结构，也无"更换 Brand Guide 触发强制复核"的存储设计 |
| G4 | **主体规模量化** | 账号数 × 平台数 × 日发布量。**YouTube 硬顶 6 条/天**，无规模数据无法验证配额是否够用 |
| G5 | **素材库与授权登记流程** | FR-5 要求"外部素材须先人工授权登记才能入库"，但该登记流程未编号 |
| G6 | **告警通道定义** | 全文只说"通知/告警"，未定义通道（邮件/IM/Webhook）与值班响应 |
| G7 | **性能与时延指标** | 只有成功率阈值，缺"brief→草稿耗時""排期→发布耗時"等时延 SLO |
| G8 | **权限模型（RBAC）** | 提到多租户隔离与"block 需管理员权限"，但无角色权限矩阵 |
| G9 | **合规词库维护流程** | "首发地区词库是上线前置条件"，但更新节奏、责任人、版本管理未定 |
| G10 | **指标字段定义** | `metrics.jsonb` 明确"M2 前定版"，属于待办风险而非需求 |
| G11 | **平台 API 变更应对流程** | 各平台政策高频变动，需常态化巡检与适配器版本管理机制 |
| G12 | **交付计划与资源估算** | 无工期、人力、预算、试点账号清单——评审无法据此排期 |

---

## 9. 关键落差：文档示例场景 ≠ 实际业务场景

> **✅ 已确认（2026-09-10）**：真实业务场景为 **B2B 工业铸件出海**。本节的落差成立，对应的平台重排、内容策略与 FR 修订见 [Pulse需求校准_B2B工业铸件场景.md](Pulse需求校准_B2B工业铸件场景.md)。

这一条是本次拆解中最重要的发现。

**文档 §4.1 的示例 brief 是快消品 B2C 场景**：

```json
{"topic": "夏季防晒新品，主打清爽不闷痘",
 "target_audience": "20-30 亚洲女性，东南亚市场"}
```

**但工作区内的实际业务资产指向完全不同的场景**：

| 证据 | 指向 |
| --- | --- |
| `菲美得产品图片\`（294 张实拍 + 19 个视频） | 铸件、加工件、壳体、阀体、箱体支座、机床件 |
| 生产流程：白模 / 发泡 / 机加工 / 涂装线 | 铸造 + 机加工制造企业 |
| `外贸公司\加急\` 12 份调研报告 | 目标客户是 **Takumi / Hurco / HMT / Macpower / STM CNC / TAKSAN 等海外机床厂商** |
| `RAG知识库\图片描述\`（快照 **327** 份 `.md`；2026-09-11 现为 **318 份素材描述 + 13 份汇总索引**，分人员/加工件/厂区场景/生产流程/铸件） | 已有素材语义描述体系，可直接喂给向量库 |

另外，源文档全文**未出现实际品牌名或实际业务描述**（"菲美得"出现 0 次），示例 brief 属写入时的占位模板，尚未与本项目业务对齐。

**这意味着三条需求需要重新标定：**

1. **平台优先级需重排。** 文档主清单把 TikTok 放首位，但 B2B 铸件采购决策者在 LinkedIn；TikTok 对工业铸件获客价值远低于 LinkedIn/YouTube。YouTube 的"6 条/天"配额对 B2B 反而够用。
2. **内容形态需重写。** 工业场景的高价值内容是**工艺过程实录、成品细节、产能与认证、质检流程**，而不是"轻快少夸张"的种草口吻。M1 的"分镜 JSON + 可编辑工程"恰好适配"工厂实拍剪辑"路线，比模板化自动成片更贴合。
3. **素材来源有独特优势。** 294 张自有实拍 = 天然规避版权风险（FR-5 的合规压力骤降），且 `RAG知识库图片描述` 已具备向量检索地基（M3 的 b-roll 语义挑选可直接复用）。

> 建议：把"业务场景基线"（B2B 工业铸件出海 vs B2C 快消）与开放问题 #1 合并评审，因为它同时决定平台主清单、语气模板、素材路线与词库。

---

## 10. 建议的下一步（按优先级）

1. **本週启动 M0 审批**：LinkedIn 组织 API、Meta App Review、TikTok 企业认证（周期最长，2–6 周）。这是唯一无法靠加班压缩的关键路径。
2. **评审前先定 3 个阻塞性问题**：首发地区与时区、单条内容成本预算、VK/俄语市场是否在首发范围。
3. **确认业务场景基线**（§9），据此重排平台优先级与语气模板。
4. **补 G1（控制台）与 G2（半自动链路）两个需求条目**，否则 M1 的"闭环"验收无法完成。
5. **把 `菲美得产品图片` + `RAG知识库图片描述` 纳入素材授权库**，作为 M0 交付物之一。
6. **量化规模**（G4）：账号数 × 平台数 × 日发布量，用 YouTube 配额反推架构是否成立。

---

*本拆解基于 v0.2.1 原文，未修改任何原文结论。缺口清单（§8）与业务落差（§9）为分析者补充。*
