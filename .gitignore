# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ---- 环境与密钥 ----
.env
.env.*
!.env.example
*.pem
*.key
# 凭据绝不入库（契约：Token 加密存储、永不落日志）
credentials*.json

# ---- 业务数据（体积大，不入代码库）----
# 素材索引（327 份描述）留在仓库目录内但入库排除；
# 原始素材 `菲美得产品图片/`、调研资料 `外贸公司/` 位于仓库上层目录，
# 天然在版本库之外（见 README §3「仓库边界」）。
RAG知识库/

# ---- 本地工具 ----
.uv-cache/
.DS_Store
Thumbs.db

# ---- 运行时数据 ----
# 素材使用台账（控制台首次启动自动生成，不入库）
.pulse/
*.lnk
